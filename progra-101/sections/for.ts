// Existen alternativas a ciclos, pero se utilizan en todos los lenguajes.
// Cantidad limitada de valores. Ej. sabemos que tenemos 50 carros. Para establecer límites.
export let limite = 100;

for (let i = 0; i <= limite; i++) {
    console.log("i = ", i);

    /*
    if (i === 5) {
        break;
    }
    */
}